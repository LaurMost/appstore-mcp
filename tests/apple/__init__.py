"""Package marker for tests/apple/ so its test modules get package-qualified
names under pytest's prepend import mode (avoids basename collisions with the
same-named modules at the tests/ root and in tests/tools/)."""
